const Footer = () => (
  <footer>
    <div>
      <em>
        Unfortunately the API does not allow the deletion of the last item.
        There has to be at least one tag.
      </em>
    </div>
  </footer>
);

export default Footer;
